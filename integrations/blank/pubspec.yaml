name: blank
description: A blank placeholder for a dart-board application
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Dart board core features
  dart_board_core:  
  
  # Debug features for dart board
  dart_board_debug:

  # A general template for bottom navigation.
  dart_board_template_bottomnav:

  # This library generates fake mock data.
  faker_dart:

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
